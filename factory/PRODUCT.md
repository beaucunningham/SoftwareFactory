# Product

Current product: hunting companion (name TBD).

Map is the primary, default tab. Guide chat is a side feature.

Product code is on Cursor Origin, not in this repository:

https://cursor.com/codebase/beau-cunningham/tmp-9883dbb9b4ecf3e0

Also known as `beau-cunningham/tmp-9883dbb9b4ecf3e0`.

`factory/config.json` field `productRepo` is that URL. SoftwareFactory keeps tickets, briefs, and the CLI. Cursor workers run on the product repo unless a brief says otherwise.
