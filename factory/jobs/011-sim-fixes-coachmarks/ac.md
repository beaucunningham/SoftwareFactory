# AC — Simulator fixes (wind + topo) + tab line + no Scout chips + coach-mark tour + Scout tutorial + patch bumps (job 011)

Owner: Sage · Implement: Kai / SoftwareFactory · iOS Sim: Lane

Copy of `AC_SIM_FIXES_COACHMARKS_v0.md`. No secrets. **Status: FINAL, approved to build.** Beau's iOS Simulator feedback on Jobs 009 + 010 (2026-09-24) is the build go, confirmed via Finley. **The AI's user-facing name stays Scout.** Bar is **Map | Pins | Forecast | Scout**. Weather and wind stay stubs. Dark default, `#BF5700` accent only, pin style A. USGS topo stays approved. No open Decisions for Beau.

**Job id:** `011-sim-fixes-coachmarks`  
**Builds on:** Jobs 009 + 010 at Origin main `6a94bc3f3926f5c08593a1b38bfe4e4f15fce624` (Jobs 009 + 010 merged, Origin PR #12)  
**Product repo:** https://cursor.com/codebase/beau-cunningham/hunting-companion

Brief: `brief.md` in this folder.  
**Build order:** E1 and E2 first. E1 must not ship broken: if the Skia wind fix is not solid by roughly half the job, ship the simpler arrow fallback (same `WindSource` stub, badge, and legend) and leave Skia disabled behind a local constant. Then E3, E4, E5, E6, E7. E7 may be dropped without blocking E1–E6.  
Lane captures the shots and recordings listed at the end of this file. Those screenshots stay out of the product repo (see `note.md`).

## Must pass

1. **E1 (BLOCKER) — Wind no longer breaks the map.** Today, with Wind on, the app keeps running but the map area goes blank (no tiles) and ignores touches. After the fix: with Wind on over **Topo, Satellite, and Standard**, tiles stay visible and the map fully responds (pan, zoom, +/−, tap-to-pin popup, pin tap popup, Map Tools, Forecast button, tabs, You menu). Overlay is see-through and never takes touches (`pointerEvents="none"` on its wrapper, or non-interactive fallback markers). Toggling Wind doesn't remount or reset the map (style, region, zoom, pins, and any open measure line stay). **Toggle Wind on/off 20 times across all three styles: map always visible and responsive within about a second, no freeze, no blank, no stuck overlay after Wind off.** Wind on for 2 minutes while panning/zooming around TX with 20+ pins: no freeze, no growing lag. Animation off the JS thread, or the fallback; no per-frame `setState`. Reduce Motion → static arrows. "Sample wind" badge + mph legend stay, clear of the bar, Map Tools, Topo attribution, and Apple legal. Pauses when backgrounded; stops fully when off. Stub `WindSource` only. If Skia can't pass, ship arrow markers / lightweight overlay instead (same stub, badge, legend). **The map must never break. Root cause in build notes.**
2. **E2 — Topo stays Topo.** Zooming in Topo never switches style or shows Apple Standard or blank. At the USGS limit (~16), max zoom is capped in Topo **or** the last tile is overzoomed (`UrlTile` `maximumNativeZ` / `shouldReplaceMapContent` / `MapView` `maxZoomLevel`, builder's choice). Leaving Topo removes any cap. Attribution stays. Pins, popup, ruler, and wind (if on) draw above topo at every zoom. **Zoom 5 → max → back at 4+ TX locations (pinch and +/−): style label and tiles stay Topo.** Same with Wind on. Root cause, cap vs overzoom, props used, and final zoom value in build notes.
3. **E3 — Selected tab is a thin light gray line.** No filled capsule, pill, or heavy outline (supersedes 009's capsule). About 1–2pt light gray line (`#D0D0D0`–`#E6E6E6` or white at ~70–85% opacity); placement, length, and color recorded in build notes. Line visible over bright Satellite and dark Standard. Selected tab clearly distinguishable. Labels/icons pass **4.5:1** over bright Satellite via the 009 halo treatment (white/semibold if orange fails). ≈150ms fade, or instant under Reduce Motion; no sliding bounce. VoiceOver announces selected. Reduce Transparency solid fallback kept with the same line. Glass material, shape, inset, 44pt targets, and Map Tools button unchanged from 009.
4. **E4 — No suggestion chips in Scout chat.** None in the empty state, after sending, under replies, or above the composer. Chip component and data feed removed, not hidden. Stub replies no longer produce follow-up chips. Empty state stays one short line plus the composer. Checked on fresh install, reinstall, after sending 1 and 5 messages, after leaving/returning, after relaunch. Chat header help button still present.
5. **E5 — Coach-mark tour.** Dims the screen (black ~60–70%) except a cutout around the real element, with a thin light outline or soft glow, a short tip bubble pointing at it, step counter, and **Skip** on every step. Steps: (1) map / tap to drop a pin, (2) Map Tools button, (3) Pins tab, (4) Forecast tab, (5) Scout tab, (6) You menu. Tapping the highlight advances (recommended: map and Map Tools steps advance only; tab steps switch tab and continue; You step finishes and returns to Map; builder records the final behavior). Taps outside the highlight do nothing. Shows once after first sign-in (new local per-install flag, e.g. `tour.coachmarks.v1`), replay from You → "App tour". Lines up on SE and Pro Max, safe areas respected, rotation handled or portrait-only noted. Missing targets are re-measured, then skipped. Reduce Motion: instant or ≤150ms fade. VoiceOver reads the tip and reaches Skip; nothing behind the scrim is focusable. In-house build recommended; free SDK 57-compatible library OK if noted; no paid/heavy library. E6 waits until the tour ends. Map Tools, tap-to-pin, and tabs work the moment the tour ends.
6. **E6 — Scout tutorial.** One example question (and one short answer only if needed), then **Try it** focuses the composer (keyboard up, cursor in the field). No "sample data" / "not your data" / "Example only" / SAMPLE wording anywhere in it. Example stays general (no invented pin names or claims about the user's data) and is never saved to chat history. Skippable. Shows once the first time Scout opens after the tour, existing 009 flag. Replay from You → "Scout tour" and chat-header help. Reduce Motion and VoiceOver respected.
7. **E7 — Patch bumps (non-blocking).** `npx expo install --fix` → expo ~57.0.25, expo-constants ~57.0.19, expo-linking ~57.0.11, expo-router ~57.0.23. Lockfile committed. `npx expo-doctor` clean (or unrelated pre-existing warnings listed). App builds and runs on the Simulator. May be dropped without blocking E1–E6.
8. **Regression:** 008 ruler drag (live segment + label, ticks, Undo/Done) on Topo and with Wind on; 008 calm motion; 008 Pins list and Map Tools button; 009 glass see-through + Reduce Transparency (with the E3 line); "Scout" everywhere, no "Guide"; 009 C6: 20 Scout prompts, none start with "birds." or a fragment, every reply a full sentence; 009 C7: 20 single taps on empty map (Satellite, Standard, Topo, after pan/zoom, after a sheet, after measure Done, and with Wind on) all open the new-pin popup; press-and-hold does nothing; 20 pin taps all open the pin popup; 010 Topo attribution; 010 7-day on top + 3-day below, aligned, Sample forecast note, Map Forecast button. Dark by default; pin style A; orange accent only; no data lost.

## Fail if

- With Wind on, the map goes blank, stops responding, freezes, or needs an app restart on any style, in any of the 20 toggles
- The wind overlay tints/fills the whole map or catches any touch
- Toggling Wind resets the map's style, region, zoom, pins, or an open measure line
- Wind ships with no Sample wind badge or legend, ignores Reduce Motion, or calls a live API
- No E1 or E2 root cause in build notes
- Topo shows Apple Standard or blank tiles at any zoom, or the style label changes while zooming
- A zoom cap set for Topo is left behind on Standard or Satellite
- A capsule, pill, or heavy outline still sits behind the selected tab, or labels fail 4.5:1 over bright Satellite
- Any suggestion chip appears anywhere in the Scout chat, or chips are only hidden
- The tour is still text-only cards, lacks Skip, lets taps outside the highlight act on the app, strands the user in a half-open sheet or wrong tab, shows before sign-in, shows every launch, or can't be replayed
- The Scout tutorial starts while the tour is showing, has more than one example, still has sample/disclaimer wording, saves the example to history, or doesn't focus the composer on Try it
- Any regression item fails ("birds." returns, tap-to-pin or pin taps drop, ruler drag broken, 7-day/3-day broken, attribution missing)
- Any live weather/wind, backend or account change, new paid SDK, key, spend, or scope creep

## Simulator sweep (Lane signoff)

1. **E1:** Map Tools → Wind on. Topo, then Satellite, then Standard: tiles visible under wind; pan, pinch, +/−, tap empty map (pin popup), tap a pin (popup), open Map Tools, switch tabs and come back. Toggle Wind on/off 20 times across the three styles, with pans/zooms between. Leave Wind on 2 minutes while moving around with 20+ pins. Background and return. Reduce Motion on: static arrows. Badge + legend present and clear of controls/attribution.
2. **E2:** Topo at 4+ TX spots; zoom 5 → max → back with pinch and +/−: stays Topo, never blank or Standard, attribution visible. Repeat once with Wind on. Switch to Standard/Satellite: normal max zoom back.
3. **E3:** Bar over bright Satellite and dark Standard: thin light gray line under/at the selected tab, no capsule; switch tabs; labels readable. Reduce Transparency on: solid bar with the line. VoiceOver: selected tab announced.
4. **E4:** Fresh install → Scout: no chips. Send 1 and 5 messages: no chips. Leave and return, relaunch: no chips.
5. **E5:** Fresh install (or new flag) → sign in → tour starts on Map. Tap outside highlights: nothing. Tap each highlight: map and Map Tools advance only; Pins/Forecast/Scout switch tabs and continue; You finishes on Map. Relaunch: no tour. You → App tour replays. Skip from a middle step: ends on Map. Repeat on iPhone SE and a Pro Max (and rotate if supported). Reduce Motion on. VoiceOver: tip read, highlight "double-tap to continue", Skip reachable. Confirm Scout tutorial did not start during step 5.
6. **E6:** After tour, open Scout: one example question (+ short answer if used), Try it → composer focused. No sample/disclaimer wording. History empty of the example after finish, Skip, and relaunch. You → Scout tour and header help both replay.
7. **E7:** `npx expo-doctor` output clean; app builds and runs.
8. **Regression:** ruler drag on Topo and with Wind on; 20 varied Scout prompts (full sentences, no "birds."); 20 tap-to-pin taps (Satellite, Standard, Topo, Wind on); 20 pin taps; Forecast 7-day + 3-day aligned with Sample forecast note; Map Forecast button; glass see-through; "Scout" everywhere.

Lane captures: **recording** of Wind on over Topo, Satellite, and Standard with a pan, zoom, tap-to-pin, and pin tap, plus off/on toggles; stills of Wind on each style (badge + legend) and Wind + Reduce Motion; **recording** of Topo zoom 5 → max → back; selected-tab line over bright Satellite and dark Standard, and Reduce Transparency on; Scout chat after sending (no chips); **recording** of the full 6-step coach-mark tour on SE and a Pro Max, plus Reduce Motion and VoiceOver stills; Scout tutorial example and Try it with composer focused; `expo-doctor` output.
