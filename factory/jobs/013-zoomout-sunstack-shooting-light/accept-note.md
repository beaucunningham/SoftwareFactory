# Accept note

Kai accepted job `013-zoomout-sunstack-shooting-light`.

- Builder, tester, security, and ui passed. G1 is Origin https://cursor.com/codebase/beau-cunningham/hunting-companion/pull/15, branch `cursor/zoomout-root-cause-8f8a`, tip `a7df6678e6f0cf760a59d63b7a19b6ddb58d42d5`. G2+G3 is Origin https://cursor.com/codebase/beau-cunningham/hunting-companion/pull/16, branch `cursor/sun-stack-shooting-light-8f8a` (docs tip `ed6d17f303fc4c7fc4379de1ecdfe09922475e10`, product tip `0f54881`, base `244b0ba`). PR #16 contains G1
- Builder: G1 first tip `f3b6ac7`, G2+G3 first tip `1524533`. Tester commits `adfcf35` (timing flake), `7662485` (Ellis rows), and `886e2de` (September test). Final G1 code tip `a7df6678e6f0cf760a59d63b7a19b6ddb58d42d5`. Final G2+G3 code tip `0f548818350a5a4e0b41bf5accdb0c7925cb0b67`. UI report `ed6d17f`
- Tester first pass failed two High items (Fabric defaulted a missing `minCenterCoordinateDistance` to 0, and `solar.ts` folded sunset onto the previous local evening) and one Medium (the popover card was `accessible`). Re-check PASSED. G1 tip `a7df667` is 249 pass / 0 fail. G2+G3 tip `0f54881` is 256 pass / 0 fail under 3 time zones with a clean typecheck. Docs tip `ed6d17f303fc4c7fc4379de1ecdfe09922475e10` is docs only
- Security PASS with no findings on re-check at G2+G3 tip `0f548818350a5a4e0b41bf5accdb0c7925cb0b67` and G1 tip `a7df6678e6f0cf760a59d63b7a19b6ddb58d42d5`. The first pass had one Low (the popover `arm()` timer leaked on repeated 'active' events). `createSecondChain` clears the pending timeout and stops when paused. No new dependencies, no new network calls, no keys or spend
- UI first run FAIL at `1524533` (G3: on a September Texas morning, sunset was placed on the previous evening, so the popover showed 'after'). The builder fixed it with a +24h roll in `sunTimesUTC`. Re-run PASS at `0f54881` (bc-89b3cda1) on Expo web at 390×844, 375×667, and 430×932. Duck before is red 0:59:30 'Until shooting light'. During (a live 10:57 AM CDT September morning) is green with a same-evening 7:32 PM sunset. After is red 'Until tomorrow's shooting light'. Deer and Squirrel end at 8:02 PM. Feral hogs show gray 'No hour limit on private land'. The note copy is exact. The countdown ticks at 1Hz and the chain stops on close. Tapping outside drops no pin. G1 stops at zoom 3 on all 3 styles with Wind on and off (55, 45, and 72 arrows at 390, 375, and 430). The badge shows only while arrows are visible. G2 sits under Forecast at 92.6×64 and a tap drops no pin. The 008–012 spot checks passed. The docs report is `docs/job-013-ui-report.md` at `ed6d17f`. Screenshots stay in the UI agent artifacts (bc-89b3cda1), not in the product repo
- G1, G2, and G3 are delivered
- G1 root cause: zoom-out had no native stop. The map set only the deprecated Apple Maps `maxZoomLevel`, and in react-native-maps 1.27.2 `cameraZoomRange` wins over the deprecated props. The fix is one stable `cameraZoomRange` with a farthest camera of 7,000 km on every style, and a Topo closest camera of 3,200 m only while Topo is selected. Standard and Satellite use a 10 m min so MapKit never gets a 0 min. Wind stays at 80 arrows or fewer, with shaft length capped at 180 km
- G3 uses the in-house NOAA math from 012, with a +24h roll in `sunTimesUTC` when sunset is at or before sunrise. The game pick is the local AsyncStorage key `shootingLight.game.v1` (id only, fallback Duck). No new dependency and no new network
- The UI worker changed no product code
- Lane's iOS Simulator list, not verifiable on web: `cameraZoomRange`, the globe, and native polylines; 10 street-to-min cycles on all 3 styles, Wind on and off, with 20+ pins and the sun stack, and a natural stop with no jolt; sun stack clearance on SE and Pro Max; popover phases, game switch, tap-outside and pan dismiss, background resync, VoiceOver, Reduce Motion, and last-game restore after kill
- Merge order: Finley squash-merges this SoftwareFactory PR, then Origin #15 (G1, `cursor/zoomout-root-cause-8f8a` at `a7df6678e6f0cf760a59d63b7a19b6ddb58d42d5`), then Origin #16 (G2+G3, `cursor/sun-stack-shooting-light-8f8a` at `ed6d17f303fc4c7fc4379de1ecdfe09922475e10`, which contains G1). Mark each ready first if it is still a draft
- Factory status is `accepted`
