# Brief — Accounts signup/login + Map-main nav

Filled by Sage (Product) for Finley / SoftwareFactory. Cursor workers do not edit this file’s intent.

**Product repo:** Origin `beau-cunningham/tmp-9883dbb9b4ecf3e0` (package `hunting-companion`) — confirm tip with Finley at job open.  
**Related locks:** `PRODUCT_NAV_LOCK_2026-09-23.md`, `ACCOUNTS_SIGNUP_LOGIN_v0.md`, `CORE_SCHEMA_v0.md`, SPECIES_TAGS_v1.

## What to build

1. **Product nav pivot:** Map is the primary/default authenticated tab (spots/pins first-class). AI Guide chat is demoted to a secondary tab (and may keep hunt-flow entry points). Guide is not deleted.
2. **Accounts:** Sign up / sign in / session restore / sign out for multi-user iOS. **Primary auth: Sign in with Apple. Secondary: email + password.** Auth backend **provider TBD** — implement `AuthSession` boundary; stub OK until BaaS unlocked. No secrets in repo or this brief.
3. **Scoping:** Local hunts/spots keyed to signed-in `userId`/`accountId` so users on one device do not see each other’s data.
4. **Onboarding:** After first auth, niche (single SPECIES_TAGS_v1) + homeState (default TX) if missing; skip if complete.

## Research

- Existing screen map had Guide as a main tab and Home CTAs including Ask guide; Beau unlocked Map-main / Guide-side on 2026-09-23.
- Prior scaffold used email/password stub only; Apple added as primary for iOS-first / store-ready path.
- BaaS/auth spend still gated historically (Finley → Morgan → Beau); hence provider TBD + stub allowed.
- Unknown until Beau confirms: email-only for this job vs Apple+email; guest/anonymous (Product recommends **no guest**).

## Acceptance criteria

- [ ] Unauthenticated launch shows auth stack only (no main tabs)
- [ ] Email sign-up and email sign-in work with clear validation errors
- [ ] Sign in with Apple works on documented Simulator/device path (or clearly gated with stub + UI present if Apple capability not yet provisioned — must not crash)
- [ ] Session persists across force-quit / relaunch
- [ ] Sign out returns to auth stack and clears session
- [ ] User A’s hunts/spots never appear while User B is signed in (same device)
- [ ] After auth, **Map** is the default selected tab; Guide is not the default
- [ ] Guide remains reachable as a secondary tab
- [ ] First-time profile (niche + homeState) still completable; returning users skip
- [ ] No API keys, tokens, or passwords committed; env placeholders only

## User-facing UI

- Auth: Welcome / Sign in / Create account; Sign in with Apple primary button; email fields secondary
- Main tabs order: **Map | Logs | Guide | You**
- You: show signed-in identity + Sign out
- Map: existing spots/pins surface as home (reuse Phase 2 map/list work; do not rebuild hunting domain in this job beyond nav + auth scoping)

## Payments and auth

- Payments: none
- Auth: Apple (primary) + email/password (secondary); provider TBD; stub session allowed
- **Secrets:** never in `factory/jobs/`, brief, or git — Finley/Beau inject via secure env outside public job tree

## Out of scope

- Password reset, magic link, SMS, Google/Facebook
- Account deletion / data export
- Shared/team accounts
- Anonymous continue / guest mode
- Live hunt sync backend, live weather, Guide model changes, App Store submit, Android focus
- Putting credentials in the job folder

## Constraints

- Keep the change small; match existing project patterns
- Add tests for new session + isolation behavior
- iOS-first Expo/RN
- Process: factory workers only (no App Desk Grok coding)

## Beau locks (2026-09-23 CT)

- Sign in with Apple: **PRIMARY** (confirmed)
- Email + password: **SECONDARY** (Product default)
- Map: **default landing tab** after auth
- Guide: side/secondary tab only
- Guest / anonymous continue: **no** — auth gates the app
- Auth backend: **stub AuthSession first**; real BaaS later via Morgan spend gate
