# Accept note

Kai accepted job `014-loghunt-parcels-sunglass-tour`.

- Builder, tester, security, and ui passed. Final Origin PRs on base `af447ef`:
  - H1 https://cursor.com/codebase/beau-cunningham/hunting-companion/pull/18, branch `cursor/h1-sun-stack-glass-8299`, tip `222154e2c0bd987b3468a34dcc0b78709ac5c2c2` (262 tests)
  - H2 https://cursor.com/codebase/beau-cunningham/hunting-companion/pull/19, branch `cursor/h2-log-a-hunt-8299`, tip `81f614a406c54e44bb5a4f2c3635c568d6c4ddc6` (264 tests)
  - H3 https://cursor.com/codebase/beau-cunningham/hunting-companion/pull/20, branch `cursor/h3-tour-autolaunch-8299`, tip `d9add6c969cd43218ee5d46feb24b679bb724240` (270 tests)
  - H4 https://cursor.com/codebase/beau-cunningham/hunting-companion/pull/21, branch `cursor/h4-property-lines-8299`, tip `143cc53b171856a8c09fbaae16a07fd2949d7495` (277 tests; includes the UI report)
- Builder: H1 tip `222154e2c0bd987b3468a34dcc0b78709ac5c2c2`. H2 tip `81f614a406c54e44bb5a4f2c3635c568d6c4ddc6`. H3 first tip `f812d36971e14a9fe2cf7a438ef3048b798a366c`, then builder fix `a1de2bb` (the Map-focus gate now only starts the tour, and replay remeasures after Map settles, with clamped cutouts). Final H3 tip `d9add6c969cd43218ee5d46feb24b679bb724240`. H4 first walk tip `c179dac8720ee155f094608494fc6f876ef7484e`, security probe-cap fix `01af522`, UI re-check `6a3e8e7`. Final H4 tip `143cc53b171856a8c09fbaae16a07fd2949d7495` includes `docs/job-014-ui-report.md`
- Tester first pass: 262 at H1 `222154e2c0bd987b3468a34dcc0b78709ac5c2c2`, 264 at H2 `81f614a406c54e44bb5a4f2c3635c568d6c4ddc6`, 268 at H3 `f812d36971e14a9fe2cf7a438ef3048b798a366c`, and 274 at the original H4 tip `c179dac8720ee155f094608494fc6f876ef7484e`, 0 fail under three time zones, with a clean typecheck. Re-check PASS. H3 has 270 tests and H4 has 277 in three time zones, `tsc` is clean, and the new H3 tests fail on the old code
- Security first pass had one Low: in-flight probes ignored the 30s cap. The probe-cap fix is `01af522`, locked by `src/ac014.h4.test.ts`. Re-check of `01af522` PASS. No new dependencies. The lockfile is unchanged
- UI first walk FAIL at `c179dac` on H3 (bc-083e34e6). HIGH: the auto-launch tour dropped at the Pins step and restarted at 1 of 8. MED: replay cutouts were off at 390 wide (You off-screen, Map Tools about 90px low). H1 passed: the Map scene stayed at opacity 1 through 20 popover cycles, game and style changes, and the tab crossfade, while other tabs still fade. Map Tools slides instead of fading, and the 0.12 floor is present. H2 passed: the Log a hunt button is 48pt, saving reorders the pin first, the zero-pin card copy is exact, and pin detail still preselects its pin. The H4 sheet passed: off by default, exact copy, zoom and outside-Texas and offline notes, persists across reload, 0 requests zoomed out. Web does not paint the ArcGIS overlay, so the device check covers that. The builder fixed H3 at `a1de2bb`. Re-check PASS at `6a3e8e7` on Expo web at 390×844, 375×667, and 430×932. The auto-launch walk goes 1 through 8 of 8 at all three sizes, and the Pins step holds at 5 of 8 on Log a hunt. Skip and relaunch leave no tour. Replay cutouts are on the controls. H1, H2, and H4 are unchanged. The docs report is `docs/job-014-ui-report.md` at H4 tip `143cc53b171856a8c09fbaae16a07fd2949d7495`. Screenshots stay in the UI agent artifacts (bc-083e34e6), not in the product repo
- H1, H2, H3, and H4 are delivered
- H1 root cause: the Map tab used `animation: 'fade'`, so expo-router's `forFade` took the scene's opacity through 0. expo-glass-effect 57.0.4 `GlassView.swift` skips the glass at low opacity and reinstalls it only while the view is unmounted. The fix keeps the Map scene at opacity 1 (`mapSceneStyle`). Map Tools slides instead of fading. A 0.12 floor tint is backup only
- The TxGIO SPEND GATE passed. Counsel reviews the redistribution terms before TestFlight. The layer is off by default. The only new network destination is the keyless HTTPS TxGIO MapServer `export`
- No new dependencies
- The UI worker changed no product code
- Lane runs the iOS Simulator steps in `docs/job-014-build.md` separately after the merge. They are not an acceptance gate
- Merge order: Finley squash-merges this SoftwareFactory PR, then Origin #18 (H1, `cursor/h1-sun-stack-glass-8299` at `222154e2c0bd987b3468a34dcc0b78709ac5c2c2`), then Origin #19 (H2, `cursor/h2-log-a-hunt-8299` at `81f614a406c54e44bb5a4f2c3635c568d6c4ddc6`), then Origin #20 (H3, `cursor/h3-tour-autolaunch-8299` at `d9add6c969cd43218ee5d46feb24b679bb724240`), then Origin #21 (H4, `cursor/h4-property-lines-8299` at `143cc53b171856a8c09fbaae16a07fd2949d7495`, which includes the UI report). Stacked PRs conflict after a squash-merge, so each later PR is rebuilt on the new main on a fresh branch with no force-push. Mark each ready first if it is still a draft
- Factory status is `accepted`
